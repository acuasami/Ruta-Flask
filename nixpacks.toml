# nixpacks.toml (Versión Final)

[phases.setup]
nixPkgs = [
  "gdal",    
  "geos"
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT app:app --workers 1"
